import 'package:flutter/cupertino.dart';

class TopReviewedScreen extends StatelessWidget {
  const TopReviewedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
